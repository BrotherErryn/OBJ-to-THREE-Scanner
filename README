Watches a directory (and its subfolders) for new or updated Lightwave OBJ models, which it will then immediately convert to THREE.js format using their Python conversion script.

Written in C# 4.0 using WPF, because that's what I enjoy using for little apps like this.